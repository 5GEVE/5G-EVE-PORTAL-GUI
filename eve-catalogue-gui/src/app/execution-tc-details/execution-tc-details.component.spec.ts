/*
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ExecutionTcDetailsComponent } from './execution-tc-details.component';

describe('ExperimentsDetailsComponent', () => {
  let component: ExecutionTcDetailsComponent;
  let fixture: ComponentFixture<ExecutionTcDetailsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ExecutionTcDetailsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ExecutionTcDetailsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/